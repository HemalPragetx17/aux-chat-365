export enum SessionState {
    ringing = "ringing",
    calling = "calling",
    talking = "talking",
    connecting = "connecting",
    ended = "ended",
    accepted="accepted"
}

export enum CallDirection {
    outbound = "outbound",
    inbound = "inbound"
}